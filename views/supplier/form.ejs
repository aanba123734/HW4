<!DOCTYPE html>
<html>
<head>
    <title><%= supplier ? 'Edit' : 'Add' %> Supplier</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="status-box">
                    <h3 class="mb-4 text-center"><%= supplier ? 'Edit Supplier' : 'Add New Supplier' %></h3>
                    
                    <form action="<%= supplier ? '/supplier/edit/' + supplier.id : '/supplier/add' %>" method="POST">
                        
                        <% if (supplier) { %>
                            <div class="mb-3">
                                <label>Supplier ID</label>
                                <input type="text" class="form-control" value="<%= supplier.supplier_id %>" disabled readonly>
                            </div>
                        <% } else { %>
                            <div class="alert alert-info py-2">Supplier ID will be auto-generated.</div>
                        <% } %>

                        <div class="mb-3">
                            <label class="fw-bold">Supplier Name</label>
                            <input type="text" name="supplier_name" class="form-control border-dark" value="<%= supplier ? supplier.supplier_name : '' %>" required>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="fw-bold">Phone Number</label>
                                <input type="text" name="phone" class="form-control border-dark" value="<%= supplier ? supplier.phone : '' %>" required>
                            </div>
                            <div class="col-md-6">
                                <label class="fw-bold">Email</label>
                                <input type="email" name="email" class="form-control border-dark" value="<%= supplier ? supplier.email : '' %>" required>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="fw-bold">Status</label>
                            <select name="status" class="form-select border-dark">
                                <option value="Active" <%= (supplier && supplier.status === 'Active') ? 'selected' : '' %>>Active</option>
                                <option value="Inactive" <%= (supplier && supplier.status === 'Inactive') ? 'selected' : '' %>>Inactive</option>
                            </select>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="/supplier/info" class="btn btn-secondary rounded-pill px-4">Cancel</a>
                            <button type="submit" class="btn btn-retro rounded-pill px-4">Save Supplier</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>