<!DOCTYPE html>
<html lang="en">
<head>
    <title>Supplier Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        /* 模仿圖片中的選單樣式 */
        .supplier-menu-list {
            list-style: none;
            padding: 0;
            margin-top: 20px;
        }
        .supplier-menu-list li {
            font-size: 2rem; /* 大字體 */
            font-weight: bold;
            margin-bottom: 10px;
        }
        .supplier-menu-list a {
            text-decoration: none;
            color: #000;
            transition: color 0.3s;
            cursor: pointer; /* 讓滑鼠變成手指 */
        }
        .supplier-menu-list a:hover {
            color: #8A2BE2; /* 紫色 hover */
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-1">
                <ul class="supplier-menu-list">
                    <li>
                        <a href="/supplier">Supplier Information</a>
                    </li>
                    <li>
                        <a href="#">Supplier Request</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>