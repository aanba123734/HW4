<!DOCTYPE html>
<html>
<head>
    <title>Edit Delivery Status</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="status-box">
                    <h2 class="mb-4">Edit Delivery Status</h2>
                    
                    <form action="/status/delivery/<%= delivery.id %>?_method=PUT" method="POST">
                        
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label text-muted">PO Number</label>
                                <input type="text" class="form-control" value="<%= delivery.po_number %>" disabled readonly>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label text-muted">Material Code</label>
                                <input type="text" class="form-control" value="<%= delivery.material_code %>" disabled readonly>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">Current Status</label>
                            <select name="status" class="form-select border-dark">
                                <option value="Processing" <%= delivery.status === 'Processing' ? 'selected' : '' %>>Processing</option>
                                <option value="On Track" <%= delivery.status === 'On Track' ? 'selected' : '' %>>On Track</option>
                                <option value="Late" <%= delivery.status === 'Late' ? 'selected' : '' %>>Late</option>
                                <option value="Delivered" <%= delivery.status === 'Delivered' ? 'selected' : '' %>>Delivered</option>
                                <option value="Cancelled" <%= delivery.status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                            </select>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="/status/delivery" class="btn btn-secondary rounded-pill px-4">Cancel</a>
                            <button type="submit" class="btn btn-retro rounded-pill px-4">Update Status</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>