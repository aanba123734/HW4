<!DOCTYPE html>
<html lang="en">
<head>
    <title>Create Menu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        /* 模仿你圖片中的選單樣式 */
        .create-menu-list {
            list-style: none;
            padding: 0;
            margin-top: 20px;
        }
        .create-menu-list li {
            font-size: 2rem; /* 大字體 */
            font-weight: bold;
            margin-bottom: 10px;
        }
        .create-menu-list a {
            text-decoration: none;
            color: #000; /* 黑色代表可點選 */
            transition: color 0.3s;
        }
        .create-menu-list a:hover {
            color: #8A2BE2; /* 紫色 hover */
        }
        .create-menu-list .disabled-option {
            color: #B0B0B0; /* 灰色代表未實作 */
            pointer-events: none; /* 禁止點選 */
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-1">
                <ul class="create-menu-list">
                    <li>
                        <a href="/create/pr">Create Purchase Request</a>
                    </li>
                    <li>
                        <a href="/create/po">Create Purchase Order</a>
                    </li>
                    
                    <li class="disabled-option">Create Material Code</li>
                    <li class="disabled-option">Create Expedite</li>
                    <li class="disabled-option">Create Payment</li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>